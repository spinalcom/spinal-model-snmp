import { SpinalNode } from 'spinal-model-graph';
export interface IDataNodes {
    graph: SpinalNode;
    organ: SpinalNode;
    context: SpinalNode;
    device: SpinalNode;
    network: SpinalNode;
    profile: SpinalNode;
}
